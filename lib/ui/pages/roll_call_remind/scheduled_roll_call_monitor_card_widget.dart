import 'package:flutter/material.dart';

class ScheduledRollCallMonitorCard extends StatelessWidget {
  const ScheduledRollCallMonitorCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}

